{
  "name": "mdothree-landing",
  "version": "1.0.0",
  "description": "Hub landing page for mdothree.com utilities",
  "scripts": {
    "dev": "node scripts/inject-config.js && npx serve public",
    "build": "node scripts/inject-config.js",
    "start": "npx serve public"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "stripe": "^21.0.1"
  }
}
